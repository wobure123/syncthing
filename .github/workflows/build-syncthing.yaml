name: Build and Release Syncthing for Ubuntu/Debian

on:
  push:
    branches:
      - release
  workflow_dispatch:

env:
  GO_VERSION: "~1.22.3"
  BUILD_USER: builder
  BUILD_HOST: github.syncthing.net
  VERSION: "v1.28.3"  # 硬编码的版本号

jobs:
  build-ubuntu-debian:
    name: Build Syncthing for Ubuntu/Debian (x86_64)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
          check-latest: true

      - name: Build Syncthing
        run: |
          go version
          GOARCH=amd64 go run build.go -version=${{ env.VERSION }}
        env:
          CGO_ENABLED: "0"

      - name: List build directory
        run: |
          ls -al
          ls -al ./bin

      - name: Archive artifacts
        uses: actions/upload-artifact@v4
        with:
          name: syncthing-ubuntu-debian
          path: ./bin/syncthing
          if-no-files-found: error
          compression-level: 6
          overwrite: false

  create-release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: build-ubuntu-debian
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: syncthing-ubuntu-debian
          path: ./syncthing-ubuntu-debian

      - name: List downloaded artifacts
        run: ls -al ./syncthing-ubuntu-debian

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ env.VERSION }}
          release_name: Release ${{ env.VERSION }}
          draft: false
          prerelease: false

      - name: Upload Release Asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./syncthing-ubuntu-debian/syncthing
          asset_name: syncthing
          asset_content_type: application/octet-stream
